#include<stdio.h>
int main()
{
    int i=0;   // in for loop when the condition is 'zero' it means false else 'one' means true.
    for(;i;)
    printf("\n here is some mail for you");
    return 0;
}
